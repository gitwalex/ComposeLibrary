<resources>
    <!-- Base application theme.
    created via https://m3.material.io/theme-builder#/custom
    -->
    <style name="Theme.Compose" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Status Bar Color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Compose.SplashScreen" parent="Theme.SplashScreen">
        <item name="android:windowBackground">@drawable/app_logo</item>
        <item name="windowActionBar">false</item>
    </style>

</resources>